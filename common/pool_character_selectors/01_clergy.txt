pool_court_chaplain = {
	valid_character = {
		# Capable adults
		is_capable_adult = yes
		is_imprisoned = no
		is_married = no
		is_betrothed = no
		# In minimum good standing with faith
		NOT = { has_trait = excommunicated }

		# Correct gender
		scope:base.faith = { has_allowed_gender_for_clergy = root }
		
		# No close family members for temporal religions unless they have relevant traits
		trigger_if = {
			limit = {
				scope:base.faith = {
					NOT = { has_doctrine_parameter = lay_clergy }
				}
			}
			OR = {
				NOT = { is_close_family_of = scope:base }
				has_trait = devoted
				has_trait = saint
			}
		}

		# No charioteers
		has_any_charioteer_trait = no 
	} 
	
	character_score = {
		base = 1
		
		modifier = {
			add = 1000
			has_same_culture_as = scope:base
		}
		modifier = {
			add = 700
			culture = { has_same_culture_heritage = scope:base.culture }
		}
		modifier = {
			add = 700
			culture = { has_same_culture_language = scope:base.culture }
		}
		modifier = {
			add = 700
			culture = { has_same_culture_ethos = scope:base.culture }
		}
		modifier = {
			add = -1000
			NOT = { culture = { has_same_culture_heritage = scope:base.culture } }
		}
		modifier = {
			add = 250
			is_lowborn = no
		}
		modifier = {
			factor = 1.5
			age > 30
		}
		modifier = {
			factor = 3
			scope:base.faith = { has_preferred_gender_for_clergy = root }
		}
	}
	
	config = {
		background = clergy
		age = { 25 55 }
	}
	
	selection_count = 25
	
	pool = faith
	gender = clergy
}

papacy_succession = {
    valid_character = {
        valid = {
            exists = global_var:preferatus.story_owner
            scope:actor = global_var:preferatus.story_owner
            actor = { has_variable = current_papacy_successor }
        }
        fail_if_no_valid = yes
    }

    character_score = {
        base = 0
        modifier = {
            add = 15000
            has_variable = current_papacy_successor
        }
    }

    config = {
       	background = cardinals
        age = { 18 85 }
    }

    selection_count = 2100000000

	court = yes
    pool = faith
    gender = clergy
}